{
  "name": "chatai-rag-web",
  "version": "1.0.0",
  "private": true,
  "description": "Small web chat that answers with Gemini using the existing LOTR RAG embeddings.",
  "type": "module",
  "scripts": {
    "dev": "npm run build && node server.js",
    "vite": "vite",
    "server": "node server.js",
    "build": "vite build",
    "start": "npm run build && node server.js"
  },
  "dependencies": {
    "vite": "^7.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "lucide-react": "^0.468.0"
  },
  "engines": {
    "node": ">=20"
  }
}
